<Window 
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Easy Onboarding CSV Generator v0.1.4" 
    Height="850" Width="850" 
    WindowStartupLocation="CenterScreen"
    Background="#F5F5F5">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#2196F3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1976D2"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#BDBDBD"/>
            <Setter Property="Background" Value="White"/>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#BDBDBD"/>
            <Setter Property="Background" Value="White"/>
        </Style>

        <Style TargetType="TabItem">
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Background" Value="#F5F5F5"/>
            <Setter Property="BorderBrush" Value="#BDBDBD"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="Border" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="1,1,1,0" 
                                CornerRadius="4,4,0,0"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter x:Name="ContentSite"
                                            ContentSource="Header"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="White"/>
                                <Setter Property="BorderBrush" Value="#2196F3"/>
                                <Setter Property="Foreground" Value="#2196F3"/>
                                <Setter TargetName="Border" Property="Background" Value="White"/>
                                <Setter TargetName="Border" Property="BorderBrush" Value="#2196F3"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="White"/>
                    <Setter Property="BorderBrush" Value="#2196F3"/>
                    <Setter Property="Foreground" Value="#2196F3"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header with Logo -->
        <Border Grid.Row="0" Background="White" CornerRadius="8" Margin="0,0,0,15" Padding="15,10">
            <Grid>
                <Grid.Effect>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="1" Opacity="0.2"/>
                </Grid.Effect>
                <StackPanel Orientation="Horizontal">
                    <Image x:Name="picLogo" Width="48" Height="48" Margin="0,0,15,0"/>
                    <TextBlock Text="Easy Onboarding CSV Generator" FontSize="24" FontWeight="SemiBold" VerticalAlignment="Center" Foreground="#212121"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Tab Control for different sections -->
        <Border Grid.Row="1" Background="White" CornerRadius="8" Margin="0,0,0,15">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="1" Opacity="0.2"/>
            </Border.Effect>
            <TabControl x:Name="mainTabControl" Margin="5" Background="Transparent" BorderThickness="0">
                <!-- Basic Information Tab -->
                <TabItem Header="Basic Information">
                    <ScrollViewer Padding="5">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Left Panel -->
                            <StackPanel Grid.Column="0" Margin="0,0,10,0">
                                <TextBlock Text="Personal Information" FontWeight="SemiBold" FontSize="16" Margin="0,0,0,10" Foreground="#424242"/>

                                <Label Content="First Name:" Margin="0,5,0,2"/>
                                <TextBox x:Name="txtFirstName" Margin="0,0,0,10"/>

                                <Label Content="Last Name:" Margin="0,5,0,2"/>
                                <TextBox x:Name="txtLastName" Margin="0,0,0,10"/>

                                <Label Content="Position:" Margin="0,5,0,2"/>
                                <TextBox x:Name="txtPosition" Margin="0,0,0,10"/>

                                <Label Content="Personal Number:" Margin="0,5,0,2"/>
                                <TextBox x:Name="txtPersonalNumber" Margin="0,0,0,10"/>

                                <Label Content="Description:" Margin="0,5,0,2"/>
                                <TextBox x:Name="txtDescription" Margin="0,0,0,10"/>

                                <StackPanel Orientation="Horizontal" Margin="0,5,0,10">
                                    <CheckBox x:Name="chkExternal" VerticalAlignment="Center"/>
                                    <TextBlock Text="External Employee" Margin="5,0,0,0" VerticalAlignment="Center"/>
                                </StackPanel>

                                <Label Content="External Company:" Margin="0,5,0,2"/>
                                <TextBox x:Name="txtExtCompany" Margin="0,0,0,10"/>
                            </StackPanel>

                            <!-- Right Panel -->
                            <StackPanel Grid.Column="1" Margin="10,0,0,0">
                                <TextBlock Text="Organizational Details" FontWeight="SemiBold" FontSize="16" Margin="0,0,0,10" Foreground="#424242"/>

                                <Label Content="Department:" Margin="0,5,0,2"/>
                                <ComboBox x:Name="cmbBusinessUnit" IsEditable="True" Margin="0,0,0,10"/>

                                <Label Content="Office/Workplace:" Margin="0,5,0,2"/>
                                <ComboBox x:Name="cmbOffice" IsEditable="True" Margin="0,0,0,10"/>

                                <Label Content="Supervisor:" Margin="0,5,0,2"/>
                                <TextBox x:Name="txtSupervisor" Margin="0,0,0,10"/>

                                <Label Content="Start Work Date:" Margin="0,5,0,2"/>
                                <DatePicker x:Name="dtpStartWorkDate" Margin="0,0,0,10"/>

                                <Label Content="Termination Date:" Margin="0,5,0,2"/>
                                <DatePicker x:Name="dtpTermination" Margin="0,0,0,10"/>

                                <Label Content="Status:" Margin="0,5,0,2"/>
                                <ComboBox x:Name="cmbStatus" IsEditable="False" Margin="0,0,0,10">
                                    <ComboBoxItem Content="Neu"/>
                                    <ComboBoxItem Content="In Bearbeitung"/>
                                    <ComboBoxItem Content="Abgeschlossen"/>
                                    <ComboBoxItem Content="Storniert"/>
                                </ComboBox>

                                <StackPanel Orientation="Horizontal" Margin="0,5,0,5">
                                    <CheckBox x:Name="chkTL" VerticalAlignment="Center"/>
                                    <TextBlock Text="Team Leader" Margin="5,0,0,0" VerticalAlignment="Center"/>
                                </StackPanel>

                                <StackPanel Orientation="Horizontal" Margin="0,5,0,10">
                                    <CheckBox x:Name="chkAL" VerticalAlignment="Center"/>
                                    <TextBlock Text="Department Head" Margin="5,0,0,0" VerticalAlignment="Center"/>
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </ScrollViewer>
                </TabItem>

                <!-- Hardware Tab -->
                <TabItem Header="Hardware">
                    <ScrollViewer Padding="10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Left Panel -->
                            <StackPanel Grid.Column="0" Margin="0,0,10,0">
                                <TextBlock Text="Computer Setup" FontWeight="SemiBold" FontSize="16" Margin="0,0,0,10" Foreground="#424242"/>

                                <Label Content="PC Model:" Margin="0,5,0,2"/>
                                <ComboBox x:Name="cmbPCModel" IsEditable="True" Margin="0,0,0,10">
                                    <ComboBoxItem Content="Standard Laptop"/>
                                    <ComboBoxItem Content="High Performance Laptop"/>
                                    <ComboBoxItem Content="Desktop PC"/>
                                    <ComboBoxItem Content="High Performance Desktop"/>
                                </ComboBox>

                                <Label Content="PC Keyboard:" Margin="0,5,0,2"/>
                                <ComboBox x:Name="cmbPCKeyboard" IsEditable="True" Margin="0,0,0,10">
                                    <ComboBoxItem Content="German (DE)"/>
                                    <ComboBoxItem Content="English (US)"/>
                                    <ComboBoxItem Content="English (UK)"/>
                                    <ComboBoxItem Content="Swiss (CH)"/>
                                </ComboBox>

                                <Label Content="Operating System:" Margin="0,5,0,2"/>
                                <ComboBox x:Name="cmbOperatingSystem" IsEditable="True" Margin="0,0,0,10">
                                    <ComboBoxItem Content="Windows 10"/>
                                    <ComboBoxItem Content="Windows 11"/>
                                    <ComboBoxItem Content="macOS"/>
                                </ComboBox>

                                <Label Content="Admin Rights Reason:" Margin="0,5,0,2"/>
                                <TextBox x:Name="txtAdminRights" Margin="0,0,0,10"/>

                                <StackPanel Orientation="Horizontal" Margin="0,5,0,10">
                                    <CheckBox x:Name="chkDockingStation" VerticalAlignment="Center"/>
                                    <TextBlock Text="Docking Station" Margin="5,0,0,0" VerticalAlignment="Center"/>
                                </StackPanel>

                                <Label Content="Monitor Count:" Margin="0,5,0,2"/>
                                <TextBox x:Name="numMonitors" Margin="0,0,0,10"/>
                            </StackPanel>

                            <!-- Right Panel -->
                            <StackPanel Grid.Column="1" Margin="10,0,0,0">
                                <TextBlock Text="Peripherals" FontWeight="SemiBold" FontSize="16" Margin="0,0,0,10" Foreground="#424242"/>

                                <Label Content="Headset Model:" Margin="0,5,0,2"/>
                                <ComboBox x:Name="cmbHeadset" IsEditable="True" Margin="0,0,0,10">
                                    <ComboBoxItem Content="Jabra"/>
                                    <ComboBoxItem Content="Logitech"/>
                                    <ComboBoxItem Content="Sennheiser"/>
                                    <ComboBoxItem Content="Other"/>
                                </ComboBox>

                                <Label Content="Other Headset Model:" Margin="0,5,0,2"/>
                                <TextBox x:Name="txtOtherHeadset" Margin="0,0,0,10"/>

                                <StackPanel Orientation="Horizontal" Margin="0,5,0,5">
                                    <CheckBox x:Name="chkWebcam" VerticalAlignment="Center"/>
                                    <TextBlock Text="Webcam" Margin="5,0,0,0" VerticalAlignment="Center"/>
                                </StackPanel>

                                <StackPanel Orientation="Horizontal" Margin="0,5,0,5">
                                    <CheckBox x:Name="chkMouse" VerticalAlignment="Center"/>
                                    <TextBlock Text="Mouse" Margin="5,0,0,0" VerticalAlignment="Center"/>
                                </StackPanel>

                                <StackPanel Orientation="Horizontal" Margin="0,5,0,5">
                                    <CheckBox x:Name="chkKeyboard" VerticalAlignment="Center"/>
                                    <TextBlock Text="Keyboard" Margin="5,0,0,0" VerticalAlignment="Center"/>
                                </StackPanel>

                                <StackPanel Orientation="Horizontal" Margin="0,5,0,5">
                                    <CheckBox x:Name="chkDeskPhone" VerticalAlignment="Center"/>
                                    <TextBlock Text="Desk Phone" Margin="5,0,0,0" VerticalAlignment="Center"/>
                                </StackPanel>

                                <Label Content="Phone Model:" Margin="0,5,0,2"/>
                                <TextBox x:Name="txtPhoneModel" Margin="0,0,0,10"/>

                                <StackPanel Orientation="Horizontal" Margin="0,5,0,5">
                                    <CheckBox x:Name="chkMobilePhone" VerticalAlignment="Center"/>
                                    <TextBlock Text="Mobile Phone" Margin="5,0,0,0" VerticalAlignment="Center"/>
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </ScrollViewer>
                </TabItem>

                <!-- Software Tab -->
                <TabItem Header="Software">
                    <ScrollViewer Padding="10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Left Panel -->
                            <StackPanel Grid.Column="0" Margin="0,0,10,0">
                                <TextBlock Text="Genesis" FontWeight="SemiBold" FontSize="16" Margin="0,0,0,10" Foreground="#424242"/>

                                <StackPanel Orientation="Horizontal" Margin="0,5,0,10">
                                    <CheckBox x:Name="chkGenesisInstall" VerticalAlignment="Center"/>
                                    <TextBlock Text="Genesis Installation" Margin="5,0,0,0" VerticalAlignment="Center"/>
                                </StackPanel>

                                <Label Content="Genesis License:" Margin="0,5,0,2"/>
                                <ComboBox x:Name="cmbGenesisLicense" IsEditable="True" Margin="0,0,0,10">
                                    <ComboBoxItem Content="Standard"/>
                                    <ComboBoxItem Content="Professional"/>
                                    <ComboBoxItem Content="Enterprise"/>
                                </ComboBox>

                                <StackPanel Orientation="Horizontal" Margin="0,5,0,10">
                                    <CheckBox x:Name="chkCommissionDependent" VerticalAlignment="Center"/>
                                    <TextBlock Text="Commission Dependent" Margin="5,0,0,0" VerticalAlignment="Center"/>
                                </StackPanel>
                            </StackPanel>

                            <!-- Right Panel -->
                            <StackPanel Grid.Column="1" Margin="10,0,0,0">
                                <TextBlock Text="Sage" FontWeight="SemiBold" FontSize="16" Margin="0,0,0,10" Foreground="#424242"/>

                                <StackPanel Orientation="Horizontal" Margin="0,5,0,10">
                                    <CheckBox x:Name="chkSageInstall" VerticalAlignment="Center"/>
                                    <TextBlock Text="Sage Officeline Installation" Margin="5,0,0,0" VerticalAlignment="Center"/>
                                </StackPanel>

                                <Label Content="Sage License:" Margin="0,5,0,2"/>
                                <ComboBox x:Name="cmbSageLicense" IsEditable="True" Margin="0,0,0,10">
                                    <ComboBoxItem Content="Basic"/>
                                    <ComboBoxItem Content="Standard"/>
                                    <ComboBoxItem Content="Professional"/>
                                </ComboBox>
                            </StackPanel>
                        </Grid>
                    </ScrollViewer>
                </TabItem>

                <!-- Telephony Tab -->
                <TabItem Header="Telephony">
                    <ScrollViewer Padding="10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Left Panel -->
                            <StackPanel Grid.Column="0" Margin="0,0,10,0">
                                <TextBlock Text="Personal Telephony" FontWeight="SemiBold" FontSize="16" Margin="0,0,0,10" Foreground="#424242"/>

                                <StackPanel Orientation="Horizontal" Margin="0,5,0,10">
                                    <CheckBox x:Name="chkPersonalExtension" VerticalAlignment="Center"/>
                                    <TextBlock Text="Personal Extension" Margin="5,0,0,0" VerticalAlignment="Center"/>
                                </StackPanel>

                                <Label Content="Personal Fax:" Margin="0,5,0,2"/>
                                <TextBox x:Name="txtPersonalFax" Margin="0,0,0,10"/>
                            </StackPanel>

                            <!-- Right Panel -->
                            <StackPanel Grid.Column="1" Margin="10,0,0,0">
                                <TextBlock Text="Group Settings" FontWeight="SemiBold" FontSize="16" Margin="0,0,0,10" Foreground="#424242"/>

                                <Label Content="Group Assignment:" Margin="0,36,0,2"/>
                                <ComboBox x:Name="cmbGroupAssignment" IsEditable="True" Margin="0,0,0,10">
                                    <ComboBoxItem Content="Sales"/>
                                    <ComboBoxItem Content="Support"/>
                                    <ComboBoxItem Content="Management"/>
                                    <ComboBoxItem Content="IT"/>
                                    <ComboBoxItem Content="Finance"/>
                                </ComboBox>
                            </StackPanel>
                        </Grid>
                    </ScrollViewer>
                </TabItem>

                <!-- Tools & Access Tab -->
                <TabItem Header="Tools &amp; Access">
                    <ScrollViewer Padding="10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <!-- Left Panel - Tool Access -->
                            <StackPanel Grid.Column="0" Grid.Row="0" Margin="0,0,10,0">
                                <TextBlock Text="Tool Access" FontWeight="SemiBold" FontSize="16" Margin="0,0,0,15" Foreground="#424242"/>

                                <Grid Margin="0,0,0,10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal">
                                        <CheckBox x:Name="chkInternalWiki" VerticalAlignment="Center"/>
                                        <TextBlock Text="Internal Wiki" Margin="5,0,0,0" VerticalAlignment="Center"/>
                                    </StackPanel>
                                    <Button Grid.Row="0" Grid.Column="1" Content="Open Link" Padding="8,3" Margin="5"/>

                                    <StackPanel Grid.Row="1" Grid.Column="0" Orientation="Horizontal" Margin="0,10,0,0">
                                        <CheckBox x:Name="chkConfluence" VerticalAlignment="Center" IsChecked="True"/>
                                        <TextBlock Text="Confluence" Margin="5,0,0,0" VerticalAlignment="Center"/>
                                    </StackPanel>
                                    <Button Grid.Row="1" Grid.Column="1" Content="Open Link" Padding="8,3" Margin="5,10,5,0"/>

                                    <StackPanel Grid.Row="2" Grid.Column="0" Orientation="Horizontal" Margin="0,10,0,0">
                                        <CheckBox x:Name="chkJira" VerticalAlignment="Center" IsChecked="True"/>
                                        <TextBlock Text="Jira" Margin="5,0,0,0" VerticalAlignment="Center"/>
                                    </StackPanel>
                                    <Button Grid.Row="2" Grid.Column="1" Content="Open Link" Padding="8,3" Margin="5,10,5,0"/>
                                </Grid>
                            </StackPanel>

                            <!-- Right Panel - Tool Details -->
                            <StackPanel Grid.Column="1" Grid.Row="0" Margin="10,0,0,0">
                                <TextBlock Text="Tool Details" FontWeight="SemiBold" FontSize="16" Margin="0,0,0,15" Foreground="#424242"/>

                                <TextBox x:Name="txtToolsDetails" TextWrapping="Wrap" AcceptsReturn="True" Height="150" Margin="0,0,0,10"/>
                            </StackPanel>

                            <!-- Left Panel - System Access (moved from Other Access tab) -->
                            <StackPanel Grid.Column="0" Grid.Row="1" Margin="0,20,10,0">
                                <TextBlock Text="System Access" FontWeight="SemiBold" FontSize="16" Margin="0,0,0,10" Foreground="#424242"/>

                                <StackPanel Orientation="Horizontal" Margin="0,5,0,5">
                                    <CheckBox x:Name="chkVPN" VerticalAlignment="Center"/>
                                    <TextBlock Text="VPN Access" Margin="5,0,0,0" VerticalAlignment="Center"/>
                                </StackPanel>

                                <StackPanel Orientation="Horizontal" Margin="0,5,0,5">
                                    <CheckBox x:Name="chkTerminalServer" VerticalAlignment="Center"/>
                                    <TextBlock Text="Terminal Server" Margin="5,0,0,0" VerticalAlignment="Center"/>
                                </StackPanel>

                                <StackPanel Orientation="Horizontal" Margin="0,5,0,5">
                                    <CheckBox x:Name="chkMS365Terminal" VerticalAlignment="Center"/>
                                    <TextBlock Text="MS365 Office on Terminal" Margin="5,0,0,0" VerticalAlignment="Center"/>
                                </StackPanel>

                                <StackPanel Orientation="Horizontal" Margin="0,5,0,10">
                                    <CheckBox x:Name="chkShop" VerticalAlignment="Center"/>
                                    <TextBlock Text="Shop / Angebotstool" Margin="5,0,0,0" VerticalAlignment="Center"/>
                                </StackPanel>
                            </StackPanel>
                            <Label Content="Other Access:" Margin="10,15,0,321" Grid.Column="1" Grid.Row="1"/>
                            <TextBox x:Name="txtOtherAccess" TextWrapping="Wrap" AcceptsReturn="True" Margin="10,43,0,113" Grid.Column="1" Grid.Row="1"/>

                            <!-- Right Panel - Processing Information (moved from Other Access tab) -->
                        </Grid>
                    </ScrollViewer>
                </TabItem>

                <!-- Notes Tab -->
                <TabItem Header="Notes">
                    <Grid Margin="10">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Text="Additional Notes" FontWeight="SemiBold" FontSize="16" Margin="0,0,0,10" Foreground="#424242"/>
                        <TextBox Grid.Row="1" x:Name="txtNotes" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>
                    </Grid>
                </TabItem>
            </TabControl>
        </Border>

        <!-- Button Panel -->
        <Border Grid.Row="2" Background="White" CornerRadius="8" Padding="15">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="1" Opacity="0.2"/>
            </Border.Effect>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button x:Name="btnSave" Content="Save to CSV" Width="120" Height="40" Margin="5"/>
                <Button x:Name="btnClose" Content="Close" Width="120" Height="40" Margin="5" Background="#757575"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
